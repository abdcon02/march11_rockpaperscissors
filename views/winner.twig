<html>
    <head>
        <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css'>
        <title>Rock Paper Scissors</title>
    </head>
    <body>
        <div class='container'>
            <h1>The winner is:</h1>
                <p> {{ winner }} </p>
                <h2>Player 1 Chose:</h2>
                    <p>
                        {{ player1 }}
                    </p>
            {% if check is not empty %}
                <h2>The Computer Chose:</h2>
                    <p>
                        {{ player2 }}
                    </p>
            {% endif %}
            {% if check is empty %}
                <h2>Player 2 Chose:</h2>
                    <p>
                        {{ player2 }}
                    </p>
            {% endif %}
                <a href="/"><button class='btn btn-danger'>Go Back</button></a>
        </div>
    </body>
</html>
